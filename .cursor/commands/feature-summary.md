Summarize the current feature and suggest next steps.

1. Read `specs/context/current-feature.md`
2. Report:
   - feature name
   - status
   - main goals
   - important notes or constraints
3. Suggest the next step:
   - `Not Started` -> run `feature start`
   - `In Progress` -> continue implementation or run `feature test`
   - `In Progress` with goals likely complete -> run `feature review`
   - `Complete` -> run `feature complete`
4. If there is no active feature, suggest loading one:
   - `No active feature. Run feature load [spec-name] to load a spec, or feature load [description] to start an inline feature.`
